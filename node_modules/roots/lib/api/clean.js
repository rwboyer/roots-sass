// Generated by CoffeeScript 1.10.0
(function() {
  var Clean, nodefn, rimraf;

  rimraf = require('rimraf');

  nodefn = require('when/node');


  /**
   * @class Clean
   * @classdesc removes a roots project's output folder
   */

  Clean = (function() {
    function Clean(roots) {
      this.roots = roots;
    }


    /**
     * Very simple function, just removed the output path and returns a promise
     * @return {Promise} promise for the removed output folder
     */

    Clean.prototype.exec = function() {
      __track('api', {
        name: 'clean'
      });
      return nodefn.call(rimraf, this.roots.config.output_path());
    };

    return Clean;

  })();

  module.exports = Clean;

}).call(this);
